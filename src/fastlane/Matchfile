# fastlane match configuration
# certificates and profiles stored in this repo under match branch

git_url(ENV["APPLE_CODE_SIGNING_GIT_URL"] || "git@github.com:tamagui/takeout2.git")
git_branch("match")

storage_mode("git")

type("appstore")

# bundle id is passed via IOS_BUNDLE_ID env var from workflow
app_identifier([ENV["IOS_BUNDLE_ID"]])

team_id(ENV["APPLE_DEVELOPER_TEAM_ID"] || "399WY8X9HY")

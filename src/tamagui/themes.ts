import { createV5Theme } from '@tamagui/themes/v5'

// amakura uses earth-toned palette customization via tamagui tokens
// the actual colors are defined in tokens.ts and applied through the theme system
export const themes = createV5Theme()
